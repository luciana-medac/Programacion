public class EjercicioBooleano {

    public static void main(String[] args) {
        
        //Tipos de variables:
        //- Números: int, float(long...)
        //- Caracteres: cahr, String
        //- Booleanos: true, false (la comparación se hace con == )

        boolean mayorEdad = true;

        if (mayorEdad) { //si no se pone nada, lo que comprueba es si es true
            System.out.println("Mayor de edad");
        } else {
            System.out.println("Menor");
        }

        //mayorEdad == true --> mayorEdad
        //mayorEdad != true --> !mayorEdad
        //Los booleanos no se pueden usar con switch

        while (mayorEdad) {
            
        }

    }
    
}
